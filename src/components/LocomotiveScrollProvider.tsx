"use client";

// Moved to ScrollProvider.tsx — this shim keeps old imports working.
export { useScroll as useLocomotiveScroll } from "@/components/ScrollProvider";
export { default } from "@/components/ScrollProvider";
