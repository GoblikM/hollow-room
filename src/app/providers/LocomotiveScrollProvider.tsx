"use client";

// Moved to ScrollProvider.tsx — this shim keeps old imports working.
export { useScroll as useLocomotiveScroll } from "@/app/providers/ScrollProvider";
export { default } from "@/app/providers/ScrollProvider";
